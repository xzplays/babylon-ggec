{
  "name": "babylon-ggec-campus",
  "version": "1.0.0",
  "private": true,
  "description": "Offline-buildable Babylon.js campus scene with first/third-person exploration controls.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@babylonjs/core": "^9.2.1",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
